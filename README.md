# products-service
This repository contains an Microservice (MS) for an e-commerce platform using NestJS as the framework and Sequelize as the ORM for MySQL. It contains a module that manage products in the database
